server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # API calls go straight to the backend's own domain (see src/main.jsx,
    # axios.defaults.baseURL) — this nginx no longer proxies anything.
    location / {
        try_files $uri /index.html;
    }
}
